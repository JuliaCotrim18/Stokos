// TipoMovimentacao.java

/**
 * Enum para representar os tipos de movimentação de estoque.
 * Usar um enum torna o código mais seguro e legível, evitando erros de digitação. 
 */

public enum TipoMovimentacao {
    ENTRADA,
    SAIDA,
    VENDA,
    AJUSTE_INVENTARIO;
}